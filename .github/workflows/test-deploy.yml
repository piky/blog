name: Test deployment

on:
  push:
    branches-ignore:
      - main
      - gh-pages
    # Review gh actions docs if you want to further define triggers, paths, etc
    # https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#on

jobs:
  test-deploy:
    name: Test deployment
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile
      - name: Test build website
        run: yarn build

  pull-request:
    name: Create Pull Request
    needs: test-deploy

    permissions: write-all

    environment: 
      name: pull-request-url
      url: ${{ steps.create_pr.outputs.pr_url }}

    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v4
      - name: Create PR
        id: create_pr
        run: |
          pr_url=$(gh pr create --title "My Pull Request #46" --body "Description" || gh pr list --head ${{ github.ref_name }} --json url -q ".[0].url")
          echo "pr_url=$pr_url" >> $GITHUB_OUTPUT
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true
      - name: Set pull-request environment variable
        run: echo "pull-request-url=${{ steps.create_pr.outputs.pr_url }}" >> $GITHUB_ENV
